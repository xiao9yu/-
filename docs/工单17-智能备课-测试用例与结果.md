# 工单17 智能备课——测试用例与结果

> 测试范围：备课数据模型、生成服务、资源检索、导出服务、API 路由（前端以 build + 手动清单验证）。
> 运行方式：`cd backend && python -m pytest -q`。

- 测试日期：2026-09-17
- 结果列说明：自动化用例写实际通过情况（✅ + 对应测试函数名）；真实 LLM 联调与浏览器手动清单尚未执行，如实标注"待人工验证"（本计划完成后用户将按清单彩排）。

## 一、后端自动化测试用例

| 编号 | 用例 | 前置 | 步骤 | 预期 | 结果 |
|---|---|---|---|---|---|
| P01 | 课程创建/查询 | teacher 登录 | POST /api/prep/courses → GET /courses | 200，列表含新课程 | ✅ `test_teacher_course_crud_and_collaborator`（创建 200，列表含新课程） |
| P02 | 学生创建课程被拒 | student 登录 | POST /api/prep/courses | 403 | ✅ `test_student_cannot_create_course`（403） |
| P03 | 非成员访问课程被拒 | teacher2 登录 | GET /courses/{id} | 403 | ✅ `test_teacher_course_crud_and_collaborator`（t2 访问 t1 课程 403） |
| P04 | 添加协作者后可见 | teacher2 被加入 | GET /courses/{id} | 200 | ✅ 同上（加协作者后 t2 访问 200） |
| P05 | 资源上传与登记 | teacher | 上传 docx | 200，course_files +1 | ✅ `test_generate_endpoint_mocks_gateway_and_uses_resources`（真实 docx 上传）+ `test_add_resource_registers_course_file`（course_files 登记） |
| P06 | 资源检索命中与引用格式 | 课程含资源 | GET /courses/{id}/search?q=梯度下降 | hits 非空，ref 形如 [1] 来源：xx 第X页 | ✅ `test_search_resources_hits_and_ref_format`（hits 非空，ref 形如 `[1] 来源：讲义.docx`；页码在解析器提供时附加，PDF 类资源含"第X页"） |
| P07 | 生成教案（mock LLM） | mock 网关 | POST /courses/{id}/generate type=plan | 结构化 JSON + 引用列表 | ✅ `test_generate_endpoint_mocks_gateway_and_uses_resources`（结构化 JSON、query 非空时 citations 非空、prompt 含课程信息与资源上下文） |
| P08 | 生成校验（缺字段） | mock 返回残缺 JSON | generate | BizError 502 提示 | ✅ `test_validators_accept_valid_and_reject_missing_keys`（plan/exercises/exam/case 缺字段均抛 BizError(502)"生成结果缺少字段"提示，请重试） |
| P09 | 教案保存/版本递增 | 教案 v1 | PUT /lessons/{id} | version=2，快照+1 | ✅ `test_lesson_save_version_restore_and_citations`（v1→v2，快照 2 条） |
| P10 | 版本恢复 | 有 v1 快照 | POST /lessons/{id}/restore {version:1} | 内容回 v1，version+1 | ✅ 同上（内容回 v1，version=3） |
| P11 | 引用保存 | lesson 存在 | POST /lessons/{id}/citations | 200，citations +1 | ✅ 同上（citations 接口 200） |
| P12 | 多媒体登记 | 文件已上传 | POST /lessons/{id}/media | 200，media_files +1 | ✅ `test_media_registration`（图片经底座 /api/files/upload 上传后登记 media_files） |
| P13 | 导出 docx 内容正确 | 教案数据 | GET /lessons/{id}/export?format=docx | 文件可读回，含标题 | ✅ `test_export_endpoint_docx`（API 层读回含"梯度下降教案"）+ `test_export_lesson_docx`（服务层读回标题与目标） |
| P14 | 导出 pptx 页数正确 | 课件数据 | GET .../export?format=pptx | 幻灯片数与数据一致 | ✅ `test_export_courseware_pptx`（幻灯片数一致，标题可读回） |
| P15 | 导出 pdf 中文正常 | 习题数据 | GET .../export?format=pdf | PDF 读回含中文 | ✅ `test_export_exercises_pdf`（PDF 读回含中文"梯度下降"与标题） |
| P16 | 生成案例（mock LLM） | mock 网关 | POST /courses/{id}/generate type=case | 六字段案例 JSON | ✅ `test_generate_case`（标题/案例背景/案例描述/问题/案例分析/结论六字段） |
| P17 | 备课数据模型建表与 CRUD | 临时库 | 七表建表 + 增删查 | 建表成功、CRUD 正常 | ✅ `test_models_prep.py` 5 个测试（course/lesson/exercise/snapshot+citation/media+coursefile） |
| P18 | 导出类型×格式守卫与临时目录清理 | 教案(plan) | GET export?format=xlsx | 400，且不产生临时目录 | ✅ `test_export_cleanup_and_invalid_format_no_temp_leak`（plan×xlsx→400；导出后无 prep_export_* 残留） |
| P19 | 协作者角色校验 | owner teacher | 添加 student/counselor/不存在用户 | 400/404，防角色旁路 | ✅ `test_add_collaborator_validates_user_and_role`（student→400、counselor→400、不存在→404） |
| P20 | LLM 未配置友好提示 | 网关抛 LLMError | POST generate | 502 + 提示文案 | ✅ `test_generate_missing_api_key_returns_502`（502，detail 含"未配置 DEEPSEEK_API_KEY"） |
| P21 | 导出 PDF 字体回退 | 无 simhei 环境（mock） | 连续两次导出 pdf | 均落盘且中文可读回 | ✅ `test_export_exercises_pdf_fallback_font_twice`（回退字体注册名缓存，连续导出不崩） |
| P22 | 未认证访问被拒 | 无/坏 token | GET /api/auth/me | 401 | ✅ `test_me_without_token_401`、`test_forged_non_numeric_sub_token_401`、`test_expired_token_401`、`test_deleted_user_token_401`（备课接口同用 get_current_user 守卫，curl E2E 亦验证备课端点 401） |
| P23 | 生成课件含讲稿（mock LLM） | mock 网关 | POST /courses/{id}/generate type=cw | prompt 含讲稿要求，结果带每页讲稿 | ✅ `test_generate_courseware_prompt_asks_for_script` |
| P24 | 课件校验缺讲稿 | mock 返回缺讲稿 JSON | generate type=cw | BizError 502 提示重试 | ✅ `test_validate_courseware_requires_script_per_slide`（缺讲稿/纯空白均 502；齐全通过） |
| P25 | 导出 pptx 备注含讲稿 | 课件含讲稿 | GET .../export?format=pptx | 备注页读回讲稿全文 | ✅ `test_export_courseware_pptx_writes_script_to_notes` |
| P26 | 旧课件导出无备注（兼容） | 课件无讲稿字段 | GET .../export?format=pptx | 无备注页，其余与现状一致 | ✅ `test_export_courseware_pptx_legacy_without_script_has_no_notes` |

## 二、前端手动功能清单（人工验证）

| 编号 | 操作步骤 | 预期 | 结果 |
|---|---|---|---|
| F01 | 教师登录 → 智能备课 → 课程列表 → 新建课程 | 课程创建成功并出现在列表 | 待人工验证（验收彩排执行；API 链路已由 P01/P02/P03/P04 覆盖） |
| F02 | 课程详情：上传资源 → 检索 → 生成教案（需配置 DEEPSEEK_API_KEY）→ 结果预览 → 保存 | 检索命中带引用标注；生成结构化教案并可保存 | 待人工验证（API 链路已由 P05/P06/P07 覆盖；真实 LLM 生成需配置 key） |
| F03 | 打开教案 → wangeditor 编辑 → 检索资源 → 一键插入引用 → 保存（版本+1） | 编辑生效，保存后版本+1 | 待人工验证（API 链路已由 P09/P11 覆盖） |
| F04 | 版本历史 → 恢复旧版本 | 内容回到旧版本，版本号+1 | 待人工验证（API 链路已由 P10 覆盖） |
| F05 | 导出 Word/PPT/PDF 并打开检查 | 文件可下载打开，内容正确 | 待人工验证（API 链路已由 P13/P14/P15/P18 覆盖） |
| F06 | 学生账号登录 → 访问备课接口被 403 拦截 | 学生只读，无法创建/操作课程 | 待人工验证（API 链路已由 P02/P19 覆盖，curl E2E 已验证 student 403） |
| F07 | 教师打开课件 → 点「数字人讲课」→ 播放/暂停/上一页/下一页/停止/静音 → 末页自动停止；旧课件出现降级提示并朗读要点 | 朵娅逐页朗读、口型同步、自动翻页；控制条行为正确；导出 PPT 打开可见备注栏讲稿 | 待彩排（用户浏览器验收后回填） |

## 三、测试结果汇总

- 后端：`python -m pytest -q` → **295 passed, 2 deselected**（2026-09-22；291 基线 + Plan F 讲课模式新增 4 用例）
- 前端：`npm run build` 通过（1696 modules，无报错）；curl E2E（Task 6）已验证：建课/上传/检索/教案 CRUD/版本恢复/media 登记/导出 docx/student 403/无 token 401，对应断言均已由上述自动化用例覆盖
- 手动清单（F01~F06）：浏览器操作与真实 LLM（DEEPSEEK_API_KEY）生成待人工验证——本计划完成后用户按清单彩排
- 自动化用例分布：备课模块 27 个（API 9 / 生成 5 / 检索 3 / 导出 5 / 模型 5）+ Plan A 公共底座 45 个 = 72

## 附：验收标准对照（完成标准 Plan B）

| 验收项 | 覆盖情况 |
|---|---|
| 五种生成可用（LLM 网关 + JSON 校验） | ✅ 生成服务测试（mock 网关）；真实 LLM 联调待人工验证（需 DEEPSEEK_API_KEY） |
| 课程资源上传 → 混合检索 → 引用标注链路 | ✅ P05/P06/P07 |
| 导出 Word/PPT/PDF 内容正确 + 类型×格式映射守卫 | ✅ P13/P14/P15/P18/P21 |
| 插入多媒体：files 落盘 + media_files 登记 | ✅ P12 |
| 版本快照：保存递增、历史列表、恢复 | ✅ P09/P10 |
| 角色权限：学生/非成员被 403 拦截 | ✅ P02/P03/P19 |
| 前端 build 通过 + 手动清单文档化 | ✅ build 通过；清单见第二节（浏览器验证留待用户，验收彩排执行） |
| 工单17 测试用例文档产出 | ✅ 本文档 |
